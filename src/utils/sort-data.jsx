// Decommisioned due to poor practices See Compendium.jsx line ~67

// export default function sortByOrder(dataArr, selectedSortDirectionStr) {
//     if (selectedSortDirectionStr === 'asc') {
//         const sortedDataArr = [...dataArr].sort((a, b) => a.pokemon > b.pokemon ? 1 : -1)
//         console.log(sortedDataArr);
//         return sortedDataArr;
//     } else {
//         return [...dataArr].sort((a, b) => b.pokemon > a.pokemon ? 1 : -1)
//     }    
// }
